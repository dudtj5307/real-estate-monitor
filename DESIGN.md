# 네이버 부동산 → 텔레그램 일일 리포트

매일 아침 9시에 지정한 아파트 단지들의 매물을 수집해, 전일 대비 **신규 / 가격변동 / 소진**을 텔레그램으로 보고한다.

- 💰 비용 **0원** (외부 서버·DB·유료 API 없음)
- 📦 의존성 2개 (`requests`, `PyYAML`)
- 🗄️ 상태 저장은 JSON 파일 1개 — 별도 DB 없음

---

## 1. 데이터 소스

네이버 부동산은 2025년 이후 `fin.land.naver.com`(Npay 부동산)으로 이전되었다.
기존에 널리 쓰이던 `m.land.naver.com/complex/getComplexArticleList` 및
`new.land.naver.com/api/*` 는 **모두 폐지**되었다 (302 리다이렉트 / 404).

### 1.1 현행 엔드포인트

```
① GET  https://fin.land.naver.com/complexes/{complexNumber}
       → 세션 쿠키 PROP_TEST_KEY / PROP_TEST_ID 획득 (워밍업)

② POST https://fin.land.naver.com/front-api/v1/complex/article/list
       Cookie: PROP_TEST_KEY=...; PROP_TEST_ID=...
       Content-Type: application/json
       Referer: https://fin.land.naver.com/complexes/{complexNumber}
       Origin:  https://fin.land.naver.com

       {
         "complexNumber": "8692",
         "tradeTypes": ["A1"],
         "size": 30,
         "userChannelType": "PC",
         "articleSortType": "PRICE_ASC"
       }
```

### 1.2 반드시 지켜야 할 3가지 (실측 확인)

| # | 제약 | 위반 시 |
|---|---|---|
| 1 | **①의 쿠키 워밍업을 먼저 수행**해야 한다 | `429 TOO_MANY_REQUESTS` |
| 2 | `complexNumber`는 **문자열** (`"8692"`) | `400` — zod 스키마가 `/^\d{1,9}$/` 문자열을 요구 |
| 3 | 페이징 파라미터는 **평면 구조** (`articlePagingRequest`로 감싸지 말 것) | `400` |

### 1.3 파라미터

| 항목 | 값 |
|---|---|
| `tradeTypes` | `A1` 매매 · `B1` 전세 · `B2` 월세 · `B3` 단기임대 |
| `articleSortType` | `PRICE_ASC` `PRICE_DESC` `DATE_DESC` `SPACE_ASC` `SPACE_DESC` `RANKING_DESC` |
| `size` | 1–30 (초과분은 `seed` + `lastInfo` 커서 페이징) |

### 1.4 응답 구조 (발췌)

```
result.totalCount / result.hasNextPage / result.seed / result.lastInfo
result.list[].representativeArticleInfo
    ├ articleNumber, complexName, dongName, tradeType
    ├ spaceInfo        : exclusiveSpace(전용㎡), supplySpace(공급㎡)
    ├ priceInfo        : dealPrice, warrantyPrice, rentPrice, managementFeeAmount
    ├ articleDetail    : floorInfo("10/20"), direction, articleFeatureDescription
    ├ verificationInfo : articleConfirmDate, verificationType
    └ brokerInfo       : brokerageName
result.list[].duplicatedArticleInfo   (선택적)
    └ realtorCount     : 동일 매물을 광고 중인 중개사 수
```

네이버가 **동일 매물의 중복 광고를 이미 그룹핑**해서 대표 매물 1건으로 내려주므로,
별도의 중복 제거 로직이 필요 없다.

### 1.5 호출 페이싱

수 초 간격 연속 호출 시 즉시 `429`가 발생한다. 따라서:

```
단지마다:  쿠키 워밍업 GET → 8초 → 매물 API POST → (다음 단지 전) 25초
```

단지 5개 기준 약 3분. 하루 1회 실행에는 무리가 없다.

#### 429는 페이싱이 아니라 IP에 걸린다 (2026-08-01 실측)

한 번 걸리면 **`front-api` 전체가 0.03초 만에 429 즉답**을 준다. 실측으로 확인한 것:

| 시도 | 결과 |
|---|---|
| 쿠키 재발급 후 재요청 | 429 |
| 쿠키 없이 요청 | 429 |
| 크롬 헤더 풀세트(`sec-ch-ua`, `priority` 등) | 429 |
| 다른 front-api 엔드포인트(`GET /complex`) | 429 |
| HTML 페이지(`/complexes/8692`) | **200** — 차단은 API 한정 |

즉 **헤더·쿠키·간격 조정으로는 풀리지 않는다.** 기다리는 것 외에 방법이 없다.

그리고 **오래 간다** — 5분 간격으로 43분을 관측하는 동안 내내 429 였다.
"잠깐 쉬었다 재시도"로 뚫을 수 있는 종류가 아니다.

우회로도 막혀 있다 — 레거시 `m.land` API(`getComplexArticleList` 등)는 폐지돼
`null`/404 를 주고, HTML 페이지에는 매물이 서버 렌더링돼 있지 않다
(`representativeArticleInfo` 가 없다). 단지 정보는 페이지에 박혀 있지만 매물은 없다.

⚠ **차단이 429 로만 오지 않는다.** 같은 차단 상태에서 curl 은 429 를 받는데
`requests` 는 응답이 아예 안 와 `ReadTimeout` 이 난다. 그래서 재시도 판정에
타임아웃·연결 실패도 포함해야 한다(`naver.RETRYABLE`). 429 만 잡으면 로컬 실행이
재시도 없이 즉사한다.

#### 차단 대응은 재시도가 아니라 '다른 IP로 다시'

⚠ **한 job 안의 재시도로는 429 를 못 뚫는다.** IP 가 그대로이고 차단이 40분을
넘기 때문이다. 오래 기다리게 만들수록 job 만 길어지고 성공률은 안 오른다.

실측 로그(2026-08-01)가 이걸 그대로 보여줬다 — 러너는 **첫 요청부터** 429 였고,
그 뒤 8분을 재시도로 태웠지만 전부 429 였다. 우리가 아무 요청도 하기 전에 이미
막혀 있던 IP 라 당연한 결과다. 그 8분은 차단된 서버를 계속 두드린 시간일 뿐이다.

그래서 재시도를 **상황에 따라 켜고 끈다** (`naver.NaverClient.ok_count`).

| 상황 | 해석 | 동작 |
|---|---|---|
| 이 실행에서 아직 **한 건도 못 받았다** | 러너 IP 가 통째로 막힌 상태 | 재시도 없이 `IPBlocked` → **남은 단지도 건너뛰고 즉시 종료** |
| 앞 단지는 됐는데 다음이 429 | 우리 호출 속도 탓일 수 있다 | 45초 → 2분, 예산 4분 안에서 재시도 |

`IPBlocked` 를 받으면 `main.py` 가 반복문을 빠져나온다. 어차피 같은 IP 라 나머지
단지도 똑같이 막히고, 두드릴수록 상황만 나빠진다. 막힌 실행은 **1분 안에 끝난다.**

그 위에 두 번째 층이 있다.

| 층 | 무엇을 흡수하나 | 구현 |
|---|---|---|
| 같은 job 안 재시도 | 순간적인 네트워크 오류·짧은 스로틀 | `naver.RETRY_WAITS`, 첫 성공 뒤에만 |
| **하루 여러 번 요청** | **IP 차단** — 아침에 막혀도 점심엔 풀려 있을 수 있다 | `refresh-request.yml` cron 2개 + `--skip-if-done` |

> **이 대목은 러너 시절과 전제가 다르다.** 당시엔 실행마다 러너 IP 가 새로 배정돼서
> "자주 던질수록 유리"했고 그래서 하루 7번을 돌렸다. 지금은 집 IP 하나뿐이라 던지는
> 횟수를 늘려도 새 IP 를 만나지 못한다. 그래서 요청을 2회로 줄였고, 다른 IP 로
> 시도하는 경로는 watchdog 폴백 하루 1회뿐이다 (§5).

재시도 예산 `RETRY_BUDGET`(4분)은 **모든 단지가 나눠 쓴다.** 단지마다 예산을 따로
주면 실행 시간이 단지 수에 비례해 늘어 워크플로 `timeout-minutes` 를 넘긴다.

#### 요청 수 자체를 줄인다 — 단지별 재개

`data/state.json`(`state.py`)이 **KST 기준** 마지막 성공일, 마지막 실패-알림일과
함께 **단지별 성공일**을 들고 있다.

- 아침에 A 는 됐고 B 가 막혔다면, 12:30 요청은 **B 만** 부른다. 요청이 줄면
  차단에 걸릴 확률도 준다.
- 건너뛴 A 는 스냅샷에서 되살려 대시보드에 남긴다(`main._from_snapshot`).
  안 그러면 재개 실행이 돌 때마다 단지가 페이지에서 사라진다.
- 전 단지가 성공한 날만 '완료'로 찍어서, 일부만 됐다면 나머지 실행이 빈 곳을
  채울 기회를 남긴다. 실패 알림은 하루 한 번만 간다.

#### 종료 코드

| 코드 | 뜻 | 워크플로 처리 |
|---|---|---|
| 0 | 정상 (또는 오늘 할 일 없음) | ✅ |
| 1 | 예상 못한 실패 | ❌ + 텔레그램 별도 알림 |
| 2 | 실행 IP 차단 | ⚠️ 경고만 — 다음 요청(12:30)이나 watchdog 폴백이 재시도 |

차단은 **예상된 상황**이라 실패로 만들지 않는다. 막힌 날마다 빨갛게 뜨면
정작 진짜 고장을 놓치게 된다. `run.sh`(Pi)와 `watchdog.yml` 둘 다 2 를 보존한다.

⚠ 실패해도 커밋 스텝이 돌아야 한다(`if: always()`). `state.json` 이 커밋되지 않으면
다음 실행이 같은 실패 알림을 또 보낸다.

### 1.6 유의사항

네이버 부동산 이용약관·robots.txt는 자동 수집을 제한한다. 본 도구는 **개인이 본인 관심
단지 소수를 하루 1회 조회**하는 용도를 전제로 하며, 위 페이싱을 준수한다. 공개 배포나
상업적 이용은 범위 밖이다.

---

## 2. 아키텍처

**실행 위치와 트리거** — 수집은 집 라즈베리파이에서 돈다 (§5 · [DESIGN-PI.md](DESIGN-PI.md)).

```
Actions  refresh-request.yml  ← 예약 KST 08:30·12:30 + 대시보드 버튼
   │     (아무 일도 하지 않는다 — 실행 기록 자체가 신호)
   │  Actions API (public·인증 불필요)
   ▼
Pi   poll.sh (2분마다) → run.sh → python -m src.main --outbox → git push
   │                                                              │
   ▼                                                              ▼
GitHub  docs/ → Pages          notify.yml (outbox.json 변경 push) → 텔레그램
                               watchdog.yml (KST 14:00) → 미수집 시 경보 + 폴백
```

**수집 한 번의 내부 흐름** — 위 `python -m src.main` 안쪽이다.

```
config.yaml                     감시 대상 단지 / 거래유형 / 평형대
        │
        ▼
src/naver.py     ── 쿠키 워밍업 → 매물 API → Article 정규화
        │
        ▼
src/diff.py      ── data/snapshot.json 의 기준과 비교 → 신규/가격변동/소진
        │            (비교는 반드시 필터 **이전**의 전체 목록으로 — 2.3)
        ▼
src/filters.py   ── 전용면적(㎡) · 평형대(공급면적) · 가격 필터 — 표시용으로만
        │
        ▼
src/report.py    ── 텔레그램 메시지 텍스트 생성 (4096자 분할)
        │
        ├──────────────┐
        ▼              ▼
src/outbox.py     src/htmlgen.py  ── docs/index.html (GitHub Pages)
   data/outbox.json    정렬·필터 가능한 표
   (전송은 Actions —
    src/telegram.py 는
    폴백·로컬 실행용)
        │
        ▼
data/snapshot.json  갱신 → git commit → push
```

### 2.0 두 가지 출력

| | 텔레그램 | HTML 대시보드 |
|---|---|---|
| 용도 | 아침에 **변화만** 빠르게 확인 | 필요할 때 **전체 매물**을 훑어보기 |
| 내용 | 신규·변동·소진 요약 | 전 매물 표 + 거래유형/평형 선택 |
| 경로 | Bot API | `docs/index.html` → GitHub Pages |

### 2.0.1 수집은 넓게, 선택은 UI에서

거래유형(매매/전세) · 평형대 · 금액대는 **수집 단계에서 좁히지 않는다**.
좁혀서 저장하면 나중에 UI에서 다시 넓힐 수 없기 때문이다. `config.yaml`은 넓게
열어두고 (`trade_types: [매매, 전세]`, `pyeong_groups: []`), 실제 선택은 대시보드에서 한다.

예외는 **면적**이다. 관심 밖 평형까지 표에 깔리면 훑어보기가 오히려 어려워져,
`defaults.area_min/max`(전용면적 ㎡, 현재 56~100)로 표시 단계에서 한 번 좁힌다.
평형대를 쓰지 않고 전용면적을 쓰는 이유는 공급면적이 단지마다 들쭉날쭉해
`30평대`가 가리키는 실제 크기가 단지 간에 다르기 때문이다. 면적은 매물이 살아
있는 동안 변하지 않으므로 금액대와 달리 경계를 넘나들어 diff를 오보하지 않고,
`filters.apply`는 비교가 끝난 뒤에만 도므로 스냅샷에는 전 매물이 남는다
(값을 되돌리면 다음 실행부터 다시 넓어진다).

**금액대를 수집 단계에서 거르면 안 되는 이유가 하나 더 있다.** `price_min/max`로
9~11억만 저장하면, 10.8억 매물이 11.5억으로 오른 날 그 매물은 범위 밖으로 나가
스냅샷에서 사라진다. diff는 이를 **"가격변동"이 아니라 "소진"으로 오보**한다.
그래서 금액대는 `site.price_focus`(대시보드 입력칸의 초기값)로만 다루고,
`defaults.price_min/max`는 정말로 영구히 버리고 싶은 매물이 있을 때만 쓴다.

표의 모든 행은 `data-trade` / `data-group` / `data-state` 속성을 갖고,
필터는 행을 숨기기만 하며 통계는 보이는 행에서 다시 계산한다.

### 2.0.2 "지금 갱신" 버튼의 한계

정적 페이지에서 브라우저가 네이버 API를 직접 호출하는 것은 불가능하다.
`fin.land.naver.com`이 CORS 헤더를 주지 않고, 세션 쿠키도 필요하기 때문이다.
서버를 두면 되지만 그 순간 이 설계의 "비용 0원" 전제가 깨진다.

대신 버튼은 **Actions 실행 화면(`refresh-request.yml`)으로 연결**한다. 거기서
`Run workflow`를 누르면 갱신 요청이 발행되고, 그 실행 기록을 감지한 집
라즈베리파이가 수집 → `docs/index.html` 재생성 → 커밋을 수행한다.
클릭 두 번이지만 추가 비용과 인증 노출이 없다.

⚠️ **누른 즉시 바뀌지 않는다.** 폴링 ≤2분 + 수집 ~3분 + Pages 반영 1~2분 =
**최대 약 7분**이다. 그래서 버튼에 소요 시간을 함께 표시한다 — 안 그러면
고장으로 읽힌다.

> 페이지에서 곧바로 워크플로를 트리거하려면 GitHub PAT가 필요한데,
> public 저장소의 정적 파일에 토큰을 넣는 것은 자격증명 유출이므로 채택하지 않는다.

HTML은 외부 CDN·폰트·스크립트를 참조하지 않는 **단일 자족 파일**로 생성한다.
파일 하나만 열면 어디서든 동작하고, GitHub Pages의 `/docs` 폴더 게시와 그대로 맞물린다.

### 2.1 상태 저장을 JSON 파일로 하는 이유

매물은 단지당 수십 건 규모라 DB가 과하다. JSON 파일 하나면 충분하고,
**git 히스토리가 그대로 가격 시계열 기록**이 되어 나중에 추이 분석을 공짜로 얻는다.
외부 DB 계정·키·무료티어 만료 관리가 사라지는 것이 이 설계의 핵심 절약 포인트다.

### 2.2 평형대 분류

API에 평형 필터가 없으므로 클라이언트에서 계산한다.
한국에서 "24평형/33평형"이라 부르는 값은 **공급면적** 기준이다.

```
평형 = supplySpace ÷ 3.3058
평형대 = (평형을 내림한 정수) // 10 * 10      예) 32.9평 → 30평대
```

### 2.3 매물 식별과 diff

#### `articleNumber` 만으로는 전부 신규가 된다

네이버가 주는 것은 개별 매물이 아니라 **중복 묶음의 대표 매물**이다
(`representativeArticleInfo` · `duplicatedArticleInfo.realtorCount`). 같은 집을
여러 중개사가 올리면 그중 하나가 대표가 되는데, **대표가 바뀌면 articleNumber 도
같이 바뀐다.** 특히 호가를 조정할 때 중개사가 기존 매물을 내리고 새로 올리는 일이
흔해서 **가격이 변한 매물일수록 새 번호로 나타난다.**

번호만 보면 그건 "소진 1건 + 신규 1건"이다. 정작 알고 싶던 가격변동이 사라지고
화면이 신규로만 채워진다. 그래서 두 단계로 짝짓는다(`diff.compare`).

| 단계 | 키 | 판정 |
|---|---|---|
| ① | `articleNumber` 동일 | 가격이 다르면 💸 가격변동 |
| ② | ①에서 남은 것끼리 **위치 키** = (거래유형, 동, 층, 전용면적) | 가격이 다르면 💸 가격변동 `↻`(재등록), 같으면 변동 없음 |
| ③ | 그래도 남은 것 | 현재분 → 🆕 신규 / 이전분 → ❌ 소진 |

②의 안전장치 둘:

- 가격 차이가 **20%(`RENUMBER_PRICE_TOLERANCE`)를 넘으면 짝짓지 않는다.** 실제
  호가 조정은 몇 % 수준이라, 그 폭을 넘으면 같은 집의 재등록이 아니라 같은 층의
  다른 호수로 보는 편이 안전하다. 짝을 못 지으면 지금까지처럼 신규 + 소진이 된다.
- 동·층·전용면적 중 하나라도 비면 서로 다른 집이 뭉치므로 ②에서 제외한다.

같은 위치 키에 여러 건이 있으면(같은 층의 다른 호수) 가격이 가까운 것부터
짝짓는다. 짝짓는 순서가 결과를 바꾸지 않도록 가격순으로 돈다.

#### 비교 기준은 '직전 실행'이 아니라 '전일'이다

하루에 여러 번 실행하므로(1.5), 매 실행이 비교 기준을 덮어쓰면 **두 번째 실행부터
전일 대비 변동이 통째로 사라진다.** 아침 리포트에 있던 신규·변동이 낮에 "지금 갱신"을
한 번 누르는 순간 대시보드에서 없어진다는 뜻이다.

그래서 `snapshot.json` 한 항목이 오늘 값과 비교 기준을 따로 들고, **기준은 날짜가
바뀔 때만 굴린다**(`diff.Snapshot`).

```json
"8692:매매": {
  "date": "2026-08-09",            // articles 를 수집한 날 (KST)
  "articles": [ ... ],             // 그날 마지막으로 본 목록
  "baseline_date": "2026-08-08",   // articles 를 비교한 기준의 날
  "baseline": [ ... ]
}
```

- 오늘 첫 실행 → 기준은 `articles`(직전 저장분). 저장할 때 그것이 `baseline` 이 된다.
- 오늘 두 번째 이후 → 기준은 `baseline` 그대로. `articles` 만 갱신한다.
- 수집이 며칠 막히면 기준일이 어제가 아니라 며칠 전이 된다. 그래서 리포트·대시보드는
  "전일 대비"라고 쓰지 않고 **실제 기준일을 그대로 보여 준다**(`report.basis`).
  기준이 아예 없으면 "첫 수집 — 전건 신규"라고 밝힌다.
- 구 포맷(매물 목록 리스트)도 읽는다. 날짜를 모르므로 기준일은 빈 값으로 둔다.

#### 필터는 비교가 끝난 뒤에 건다

`filters.apply`를 수집 직후에 걸면 필터 통과분만 스냅샷에 남아, 경계를 넘나든
매물이 가격변동이 아니라 소진 + 신규로 오보된다(2.0.1). 그래서 **저장·비교는 전체
목록으로 하고, 좁히기는 표시 직전에만** 한다(`main._section`).

---

## 3. 설정

```yaml
telegram:
  chat_id: "123456789"          # 토큰은 환경변수 TELEGRAM_BOT_TOKEN

defaults:
  trade_types: [매매]            # 매매 / 전세 / 월세 / 단기임대
  area_min: 56                  # 전용면적 ㎡ 하한, null이면 제한 없음
  area_max: 100                 # 전용면적 ㎡ 상한
  pyeong_groups: [20, 30]       # 공급면적 기준 평형대
  price_max: null               # 만원 단위, null이면 제한 없음

complexes:
  - name: 성복역현대홈타운
    number: "8692"
```

- `defaults` + 단지별 override 구조라 단지를 늘려도 설정이 짧게 유지된다.
- 전세를 함께 보려면 `trade_types: [매매, 전세]` 한 줄만 고치면 된다.

---

## 4. 리포트 형태

```
🏠 부동산 리포트 · 2026-07-31 (금)

━━ 성복역현대홈타운 ━━
💰 매매

[30평대] 13건 (▲1) · 10.00~12.30억
  🆕 205동 10/20층 · 11.50억
     샷시포함확장완전특올수리, 성복역초역세권
  💸 202동 중/20층 · 11.50억 → 11.20억 (-3,000만)
  ❌ 소진 1건

[20평대] 5건 (—) · 10.00~11.00억
     변동 없음
```

신규/가격변동/소진만 상세 표시하고 나머지는 요약해 메시지 길이를 억제한다.

---

## 5. 실행 환경

| | **라즈베리파이 워커 (현행·권장)** | GitHub Actions 공용 러너 | Windows 작업 스케줄러 |
|---|---|---|---|
| 비용 | 0원 (전기 월 수백 원) | 0원 | 0원 |
| 전원 | Pi 상시 가동 | 무관 ✅ | 9시에 PC 가 켜져 있어야 |
| 차단 리스크 | **낮음 — 집 IP** | **높음 — 실측 확인** (공용 러너 IP) | 낮음 — 집 IP |
| 정시성 | cron 지연 + 폴링 ≤2분 | 5~30분 지연 가능 | 정확 |
| 대시보드 자동 갱신 | ✅ | ✅ | ❌ (직접 커밋) |

**2026-08-01 첫 Actions 실행이 첫 요청부터 429** 로 실패했다. 우리 코드가 그 IP 에서
아무 요청도 하기 전이었으므로, 러너 IP 가 이미 다른 사용자들 때문에 차단돼 있었다는
뜻이다. 공용 IP 라 매일 달라지므로 되는 날도 있고 안 되는 날도 있다.

**그래서 실행 위치를 집 라즈베리파이로 옮겼다.** 전체 설계는
[DESIGN-PI.md](DESIGN-PI.md) 에 있고, 핵심만 옮기면 이렇다.

1. **Actions 는 요청만 발행한다** (`refresh-request.yml`, KST 08:30·12:30 +
   대시보드 버튼). 아무 일도 하지 않는 워크플로이고, **실행 기록 자체가 신호**다.
2. **상시 가동 중인 Pi 가 2분마다 그 실행 id 를 폴링**해 수집하고 결과를 push 한다.
   인바운드 연결이 없으므로 포트 개방·DDNS 가 필요 없다.
3. **텔레그램 전송은 다시 Actions 가 한다** (`notify.yml`). 24시간 켜 두는 집
   기기에 토큰을 두지 않기 위해서다. Pi 는 완성된 메시지를 `data/outbox.json` 에
   적어 push 할 뿐이다. 그래서 **push 가 알림의 유일한 경로**다.
4. **Pi 가 죽은 날은 `watchdog.yml`(KST 14:00)이 경보를 보내고 공용 러너로 한 번
   대신 수집한다.** 러너 IP 가 막혀 있으면 이 폴백도 실패하지만, 적어도 "오늘
   데이터가 없다"는 사실은 집 밖에서도 알 수 있다.

핵심은 여전히 **실행 IP 를 바꾸는 것**이다. 헤더·쿠키·간격으로 푸는 길은 실측으로
막혀 있다(§1.5). 프록시로 IP 만 갈아 끼우는 방법도 있지만, 데이터센터 대역은
애초에 차단 대상이라 러너 IP 와 같은 처지이고 "비용 0원 · 자격증명 없음" 전제를 깬다.

> **self-hosted 러너는 채택하지 않았다.** 실행 IP 를 집으로 옮기는 목적은 같지만,
> 이 저장소는 public 이라 외부 PR 의 코드가 집 기기에서 실행될 수 있다. pull 모델은
> Pi 가 자기 저장소의 `main` 만 받아 실행하므로 그 경로가 없다.

- 요청 cron (UTC): `30 23` (KST 08:30) / `30 3` (KST 12:30)
- 감시 cron (UTC): `0 5` (KST 14:00) — 두 요청이 모두 지나간 뒤라야 미수집이 확정된다
- 요청 워크플로가 매일 도는 덕분에 **60일 무활동 자동 비활성화도 회피**된다
- 바뀌지 않은 것: **호출 페이싱(§1.5)과 재시도 정책.** 집 IP 는 바뀌지 않으므로
  오히려 두드리지 않는 것이 전보다 더 중요해졌다

---

## 6. 준비물

| 항목 | 방법 |
|---|---|
| 봇 토큰 | `@BotFather` → `/newbot` |
| chat_id | 봇에게 메시지 전송 후 `api.telegram.org/bot<TOKEN>/getUpdates` |
| GitHub Secret | repo Settings → Secrets → `TELEGRAM_BOT_TOKEN` |
| 단지번호 | `fin.land.naver.com/complexes/{번호}` URL의 숫자 |

---

## 7. 실패 처리

- 단지 단위로 예외를 격리해, 한 단지가 실패해도 나머지는 정상 보고한다.
  단, `IPBlocked`(실행 IP 차단)는 예외다 — 나머지 단지도 같은 IP 라 볼 것 없다.
- 수집이 **전부** 실패하면 스냅샷을 덮어쓰지 않는다 (다음 날 전건 신규로 오보되는 것 방지).
- 실패 내역은 리포트 하단에 함께 전송해 조용히 죽는 상황을 막는다.

### 7.1 알림이 조용히 사라지지 않게

텔레그램 설정이 비어 있으면 리포트는 콘솔로만 나가고 실행은 성공으로 끝난다.
실제로 secret 이 전달되지 않아 **아무 알림도 못 받은 채 잘 돌고 있다고 믿은** 적이
있다. 그래서 세 겹으로 막는다.

| 겹 | 무엇을 잡나 |
|---|---|
| 워크플로 preflight | secret 이 비었으면 수집 전에 `::error::` 로 실패시킨다 |
| `Telegram.warn_if_disabled()` | 무엇이 비었는지(`TELEGRAM_BOT_TOKEN`/`_CHAT_ID`) 이름을 찍는다 |
| `--test-telegram` | `getMe` 로 토큰을, 실제 전송으로 chat_id 를 확인한다 |

전송 실패(HTTP 401/400/403)는 사유를 그대로 해석해 준다 — 토큰 오류인지,
chat_id 오류인지, 봇이 차단된 건지. 429·5xx 는 `retry_after` 만큼 기다렸다 재전송한다.

파이썬이 예외로 죽으면 앱이 보내는 실패 알림도 못 나가므로, 그 경우에만
워크플로가 `curl` 로 직접 알린다(`if: failure()`). 429 차단(코드 2)은 여기서 제외된다 —
예상된 상황이고 앱이 이미 하루 한 번 알렸다.
